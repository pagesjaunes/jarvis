<?xml version="1.0" ?>

<container xmlns="http://symfony.com/schema/dic/services"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://symfony.com/schema/dic/services http://symfony.com/schema/dic/services/services-1.0.xsd">

    <services>

        <service
            id="command.cache.clear"
            class="Jarvis\Command\Core\CacheClearCommand"
        >
            <call method="setContainer">
                 <argument type="service" id="service_container" />
            </call>

            <tag name="console.command" />
        </service>
        <service
            id="command.container.debug"
            class="Jarvis\Command\Core\ContainerDebugCommand"
        >
            <call method="setContainer">
                 <argument type="service" id="service_container" />
            </call>

            <tag name="console.command" />
        </service>
        <service
            id="command.container.self_update"
            class="Jarvis\Command\Core\SelfUpdateCommand"
        >
            <tag name="console.command" />
        </service>
    </services>
</container>
